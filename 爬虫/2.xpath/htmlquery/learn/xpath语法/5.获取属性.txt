获取属性
    前面都是获取标签的方法，会获取整个标签
    不能单独获取标签里面的属性获取内容

    而xpath有语法可以获取标签中的属性值


语法
    格式：获取标签/@属性

    前面是要获取标签的路径
    后面直接@属性就可以获取最后一个标签的属性了


示例
    //div/a/@href   --   获取a标签属性值
    //a/@href       --   获取所有a标签的属性值